import type { DataAccess, DataOffer, PendingDeal } from "../domain/types";
import { Icon } from "./Icons";

export function DealPanel({ offer, preview, access, walletCredits, budgetCredits, durationDays, committing, approved, canUndo, onPreview, onApprove, onCommit, onCancel, onUndo }: { offer: DataOffer; preview: PendingDeal | null; access?: DataAccess; walletCredits: number; budgetCredits: number; durationDays: number; committing: boolean; approved: boolean; canUndo: boolean; onPreview: () => void; onApprove: () => void; onCommit: () => void; onCancel: () => void; onUndo: () => void }) {
  if (preview) return <section className="deal-panel deal-panel--preview" aria-label="Deal preview"><div className="deal-heading"><Icon name="spark" size={28}/><span><small>{preview.negotiation === "countered" ? "Seller counteroffer" : "Bid accepted"}</small><h2>{preview.outcomeTitle}</h2></span></div><p>{preview.rationale}</p><dl className="deal-terms"><div><dt>Your bid</dt><dd>{preview.bidCredits} credits</dd></div><div><dt>Agreed price</dt><dd>{preview.agreedCredits} credits</dd></div><div><dt>Access</dt><dd>{preview.durationDays} days</dd></div><div><dt>License</dt><dd>Commercial</dd></div></dl><div className="deal-changes">{preview.changes.map((change) => <div key={change.label}><small>{change.label}</small><span>{change.before ? <del>{change.before}</del> : null}<strong>{change.after}</strong></span></div>)}</div><div className="deal-actions">{approved ? <button className="button button--primary" type="button" onClick={onCommit} disabled={committing}>{committing ? "Unlocking…" : "Commit approved rental"}</button> : <button className="button button--primary" type="button" onClick={onApprove}>Approve exact deal · {preview.agreedCredits}</button>}<button className="button button--secondary" type="button" onClick={onCancel} disabled={committing}>Cancel</button></div><p className="approval-note"><Icon name="lock" size={15}/> {approved ? "Human approval recorded for this exact token." : "No credits are used until you approve."}</p></section>;
  if (access) return <section className="deal-panel deal-panel--access" aria-label="Data access granted"><div className="deal-heading"><Icon name="check" size={30}/><span><small>Access granted</small><h2>{access.title}</h2></span></div><p>{access.durationDays}-day commercial rental from {access.seller}. The agent can now use these machine-ready fields.</p><div className="schema-preview"><small>Unlocked schema</small>{access.sampleFields.map((field) => <code key={field}>{field}</code>)}</div><div className="access-meta"><span>Paid <strong>{access.paidCredits} credits</strong></span><span>Wallet <strong>{walletCredits} credits</strong></span></div><button className="button button--secondary button--full" type="button" onClick={onUndo} disabled={!canUndo}><Icon name="undo" size={17}/> Reverse demo deal</button></section>;
  return <section className="deal-panel" aria-label="Goal and budget"><h2>Goal &amp; budget</h2><dl className="brief"><div><dt>Goal</dt><dd>Choose a Sydney location for a weekend pop-up</dd></div><div><dt>Budget</dt><dd>Up to {budgetCredits} credits</dd></div><div><dt>Duration</dt><dd>{durationDays} days</dd></div><div><dt>Available</dt><dd><Icon name="credits" size={17}/>{walletCredits} credits</dd></div></dl><div className="selected-deal"><small>Review recommendation</small><strong>{offer.title}</strong><span>{offer.seller} · {offer.rentalCredits} credits listed</span><button className="button button--primary button--full" type="button" onClick={onPreview}>Negotiate rental <span>→</span></button></div><p className="approval-note"><Icon name="lock" size={15}/> Your agent can negotiate. You approve the deal.</p></section>;
}
